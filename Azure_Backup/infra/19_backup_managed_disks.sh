#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/00_prereqs.sh"
# Example: Protect a disk independent of VM context (preview/GAs vary by region).
echo "See module for Managed Disk backup steps."
